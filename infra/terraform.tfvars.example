# Copy to terraform.tfvars and fill in real values.
# This file contains NO SECRETS — only infrastructure config.
# Secrets go in ../secrets.env (see secrets.env.example).

vault_repo            = "yourorg/lobmob-vault"
wg_manager_public_key = "run: lobmob init (generates this)"
ssh_pub_key_path      = "~/.ssh/id_ed25519.pub"
region                = "nyc3"
project_name          = "lobmob"
# manager_size        = "s-2vcpu-4gb"   # uncomment to override default
# worker_size         = "s-1vcpu-2gb"   # uncomment to override default
