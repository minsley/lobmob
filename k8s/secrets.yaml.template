# Template only — do NOT commit actual secrets.
# Create the real secret with:
#   kubectl create secret generic lobmob-secrets -n lobmob --from-env-file=secrets.env
#
# Or apply this template after filling in base64-encoded values:
apiVersion: v1
kind: Secret
metadata:
  name: lobmob-secrets
  namespace: lobmob
type: Opaque
data:
  ANTHROPIC_API_KEY: ""      # base64
  DISCORD_BOT_TOKEN: ""      # base64
  GH_APP_ID: ""              # base64
  GH_APP_PRIVATE_KEY: ""     # base64
  DIGITALOCEAN_TOKEN: ""     # base64
