# Copy to secrets.env (prod) or secrets-dev.env (dev) and fill in real values.
# NEVER commit secrets files — they are gitignored.
#
# Push to k8s: lobmob secrets push         (lobmob-secrets)
#              lobmob secrets push-broker   (lobwife-secrets)

# DigitalOcean API token (also exported as DIGITALOCEAN_TOKEN for terraform)
DO_TOKEN=dop_v1_...

# GitHub fine-grained PAT scoped to the vault repo (used by lobboss init container)
GH_TOKEN=github_pat_...

# Discord bot token
DISCORD_BOT_TOKEN=MTI...

# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Google Gemini API key (optional, for image-gen lobsters)
GEMINI_API_KEY=

# --- GitHub App credentials (lobwife token broker) ---
# Created via: lobmob setup github
# Pushed to k8s via: lobmob secrets push-broker -> lobwife-secrets

# GitHub App ID (numeric)
GH_APP_ID=

# GitHub App installation ID (numeric, set after installing the App on repos)
GH_APP_INSTALL_ID=

# Base64-encoded GitHub App PEM private key
# Encode: base64 < your-app.pem
GH_APP_PEM=
