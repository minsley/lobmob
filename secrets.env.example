# Copy to secrets.env and fill in real values.
# NEVER commit secrets.env — it is gitignored.
#
# These secrets are pushed to lobboss via SSH after deploy.
# They never appear in Terraform state or cloud-init user_data.

# DigitalOcean API token (also exported as DIGITALOCEAN_TOKEN for terraform)
DO_TOKEN=dop_v1_...

# GitHub fine-grained PAT scoped to the vault repo
GH_TOKEN=github_pat_...

# Discord bot token
DISCORD_BOT_TOKEN=MTI...

# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Base64-encoded private deploy key for the vault repo
# Generate: ssh-keygen -t ed25519 -f ~/.ssh/lobmob_deploy -N ""
# Encode:   base64 < ~/.ssh/lobmob_deploy
VAULT_DEPLOY_KEY_B64=LS0t...

# WireGuard private key for lobboss (generated by lobmob init)
WG_LOBBOSS_PRIVATE_KEY=
